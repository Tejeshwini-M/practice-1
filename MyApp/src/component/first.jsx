import React, { Component } from 'react' //class component(rcr) - there are two components

export default class first extends Component {
  render() {
    return (
      <div>first</div>
    )
  }
}
